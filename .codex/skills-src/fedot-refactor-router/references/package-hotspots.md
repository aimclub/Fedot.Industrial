# Package Hotspots

## `fedot_ind/core/models/kernel`

Often routes to:

- `$fedot-pure-core-shell`
- `$fedot-typed-domain-errors`
- `$fedot-invariant-tests-review`

Clues:

- forecaster orchestration
- window or q policy modeling
- prediction diagnostics
- method normalization

## `fedot_ind/core/operation/decomposition`

Often routes to:

- `$fedot-pure-core-shell`
- `$fedot-invariant-tests-review`
- `$fedot-typed-domain-errors`

Clues:

- OKHS or DMD numerical core
- matrix assembly
- spectral policies
- decomposition invariants

## `fedot_ind/core/operation/transformation`

Often routes to:

- `$fedot-pure-core-shell`
- `$fedot-safe-configs`
- `$fedot-invariant-tests-review`

Clues:

- representation transforms
- kernel parameter normalization
- trajectory preprocessing
- data-to-feature mapping rules

## `fedot_ind/core/models/automl`

Often routes to:

- `$fedot-safe-configs`
- `$fedot-typed-domain-errors`
- `$fedot-invariant-tests-review`

Clues:

- benchmark configs
- rolling forecast policies
- product-layer summaries
- result schemas and serialization

## `benchmark/v2`

Often routes to:

- `$fedot-extension-contract`
- `$fedot-safe-configs`
- `$fedot-invariant-tests-review`

Clues:

- dataset adapters
- model adapters
- manifests and presets
- registry and publication artifacts
- optional external baselines

## `examples/rkhs_okhs` and `docs/okhs_dmd`

Often routes to:

- `$fedot-pure-core-shell`
- `$fedot-invariant-tests-review`
- `$fedot-refactor-router`

Clues:

- experiment runner cleanup
- diagnostics and visualization boundaries
- documentation-driven refactors
- code and theory alignment
