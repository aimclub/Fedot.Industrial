# Kernel Learning result root

This directory is intentionally tracked as a manifest-only placeholder for CI and local benchmark resumption.
Large Kernel Learning run outputs are external artifacts and must not be committed.