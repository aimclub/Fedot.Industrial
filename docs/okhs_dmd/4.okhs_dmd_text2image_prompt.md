# OKHS DMD Figure Text2Image Prompt

## Prompt

```text
Create a single large publication-quality scientific infographic that explains the full end-to-end workflow of an OKHS-based DMD forecasting model with projected latent trajectory representation and adaptive latent trajectory thinning. The figure must look like an ICML-style scientific figure, clean academic vector infographic, white background, muted blue-gray palette with one accent color, minimal typography, precise arrows, thin lines, labeled panels, no photorealism, no 3D glossy rendering, no decorative background, conference-paper figure aesthetics, mathematically clean, visually balanced.

Overall visual style:
- landscape orientation, wide multi-panel composition, conference-paper quality
- crisp vector look, no blur, no painterly texture, no cartoon style
- white background, dark gray text, muted blue-gray blocks, one restrained accent color such as teal or orange for the projected path
- thin arrows, subtle separators, compact labels, small mathematical annotations
- balanced whitespace, aligned panels, no clutter, no unnecessary icons
- visually emphasize the projected-latent path as the main method and show older branches only as faint comparison branches

Main title:
"OKHS DMD Forecasting Pipeline With Projected Latent Trajectories"

Small subtitle:
"Adaptive windowing, low-rank projection, multivariate latent trajectory construction, FractionalDMD training, latent forecast decoding, and benchmark diagnostics"

Use 8 clearly labeled panels arranged from left to right or in two balanced rows. Every panel should have a short title and 1-3 compact visual annotations. Use mathematical labels only where they help understanding. The overall figure should read like a polished machine learning methods figure in a paper.

Panel A: Input time series and forecasting task
- Show a univariate time series with a long training segment and a short forecast horizon at the right edge.
- Mark the train/forecast boundary with a dashed vertical line.
- Label the last observed point and the forecast start.
- Add a small horizon annotation and a compact series id tag, like an M4 example.
- Show that the task is to map the observed history into a multi-step forecast.
- Use one clean line plot only, with a boundary zoom inset drawn very simply.

Panel B: Adaptive window selection in the original space
- Show two conceptual window choices above the series: a tiny fixed window and a larger adaptive window.
- The tiny window should be shown in a faint gray crossed-out comparison box.
- The adaptive window should be highlighted.
- Add short labels:
  "fixed tiny window rejected"
  "adaptive window policy"
  "window fraction 10%-25%"
  "cycle-aware candidate"
  "resolved window size"
- Make it visually clear that the method avoids an excessively small window because it causes redundant local trajectories and weak global structure.

Panel C: Original trajectory matrix construction
- Show overlapping windows extracted from the original time series and stacked into a 2D trajectory matrix X.
- Draw X as a matrix with rows as time windows and columns as lag positions.
- Add a small note:
  "original trajectory matrix X"
  "rows = windows"
  "columns = lag positions"
- Also show that original-space stride can reduce extreme overlap before compression.
- Include a tiny annotation for "trajectory sampling stride" and "effective trajectory count".
- The panel should communicate that the raw time series is transformed into structured windows before any spectral modeling.

Panel D: SVD, rank selection, and representation policies
- Show the decomposition X = U S V^T as a central mathematical block.
- Under it, show three policy branches:
  1. none
  2. reconstructed
  3. projected
- Make "projected" the clearly highlighted active branch.
- For "reconstructed", show:
  U_r S_r V_r^T -> smoothed window matrix
- For "projected", show:
  Z = U_r S_r
  V_r^T = decode basis
- Add compact annotations:
  "explained dispersion"
  "rank floor"
  "selected rank r"
  "projected is default for adaptive DMD"
- Visually explain that reconstructed windows can over-smooth local dynamics, while projected representation keeps a compact latent evolution.

Panel E: Multivariate latent state sequence and latent trajectory tensor
- This is the most important panel and should be visually dominant.
- Show the latent state sequence Z as a 2D matrix or ordered strip with shape (T_latent, r), where each row is a latent vector.
- Then show how sliding latent windows are built from Z.
- The resulting object must be drawn as a clean scientific tensor schematic with shape (N, L, r).
- Important: do not render flashy 3D art; instead show a restrained stacked-slice tensor with thin outlines and clear axis labels.
- Label:
  "multivariate latent trajectories"
  "shape N x L x r"
  "N = number of latent trajectories"
  "L = latent window length"
  "r = latent dimension"
- Add a clear note that each time step inside a trajectory is a latent vector, not a scalar.
- Show latent trajectory thinning:
  "dense_latent_trajectory_count -> effective_latent_trajectory_count"
  "latent stride policy"
  "adaptive latent stride"
  "reduce redundancy without changing rank"
- Add a compact diagnostics box:
  "latent_window_size"
  "latent_stride"
  "latent_overlap_ratio"
  "latent_state_count"
  "effective_latent_trajectory_count"
- The panel must make it obvious why the training object is now a 3D tensor rather than a 2D matrix of scalar windows.

Panel F: OKHS and FractionalDMD training in latent space
- Show the latent trajectory tensor flowing into three vertically stacked scientific modules:
  1. OKHS transformer
  2. Fractional Liouville operator
  3. FractionalDMD
- For the OKHS transformer show:
  "Gram matrix G"
  "occupation-kernel representation"
  "kernel interactions between latent trajectories"
- For the Fractional Liouville operator show:
  "Liouville matrix A"
  "fractional spectral operator"
  "quadrature-based operator construction"
- For FractionalDMD show:
  "eigenvalues lambda_j"
  "modes Phi_j"
  "initial coefficients c_j"
  "prediction-mode selection"
- Add a small complex-plane inset with eigenvalue dots and a tiny mode-score bar chart.
- Use thin arrows:
  latent trajectories -> Gram matrix -> Liouville operator -> FractionalDMD fit
- Emphasize that training happens in latent coordinates, not directly on reconstructed windows.

Panel G: Latent-space prediction, decoding, and forecast extraction
- Show the last observed segment being mapped into latent coordinates.
- Then show a latent prediction block:
  "Z_pred"
- Then decode through:
  "X_pred ~= Z_pred V_r^T"
- Then extract the scalar forecast from decoded windows.
- Visualize this as a compact four-step chain:
  "project observed segment"
  "predict in latent space"
  "decode to window space"
  "read out forecast values"
- Add a small diagnostics box:
  "latent_prediction_shape"
  "decoded_prediction_shape"
  "decode_reconstruction_error"
  "latent_first_prediction_value"
  "decoded_first_prediction_value"
- Optionally show a tiny faded comparison note:
  "projected path avoids double smoothing of reconstructed windows"

Panel H: Final forecast and benchmark diagnostics
- Show the original train history, the actual future segment, and multiple model forecasts on a shared plot.
- Highlight the OKHS DMD forecast in the accent color.
- Include a boundary zoom inset that emphasizes continuity and local slope near forecast start.
- Add a compact artifact card:
  "history_forecast_overlay"
  "boundary_zoom"
  "okhs_diagnostics.json"
  "MAE / sMAPE / MASE"
- Add a second tiny card:
  "targeted issues"
  "boundary jump"
  "trajectory redundancy"
  "over-smoothing"
  "latent trajectory explosion"
- This panel should communicate that the full pipeline produces both forecasts and interpretable diagnostics suitable for benchmark studies and scientific papers.

Notation and legend:
- X = original trajectory matrix
- Z = latent state sequence
- V_r^T = decode basis
- G = OKHS Gram matrix
- A = Fractional Liouville matrix
- Phi_j = DMD modes
- N x L x r = latent trajectory tensor
- use muted blue-gray for standard flow
- use accent color for projected-latent path
- use dashed gray arrows for inactive comparison branches such as reconstructed representation

Important scientific emphasis:
- The central conceptual message of the figure must be the transition from reconstructed windows to projected latent states.
- The most visually important technical novelty must be the construction of multivariate latent trajectories as a 3D tensor.
- The second most important novelty must be training the OKHS -> Fractional Liouville -> FractionalDMD stack directly on those latent trajectories.
- The third key idea must be latent prediction followed by decode through V_r^T back to forecasted signal windows.
- The figure should feel like a high-quality methods figure in an ICML or NeurIPS paper, not a product diagram.

Do not include:
- photorealistic objects
- decorative background patterns
- glossy 3D cubes
- colorful dashboard styling
- heavy shadows
- long paragraphs
- oversized text

Use concise labels, mathematically clean structure, and conference-paper aesthetics throughout.
```

## Notes

- Main emphasis for the image generator: Panel E and Panel F.
- If the image model struggles with text rendering, keep formulas short and preserve the structure of the panels.
- If a more compact one-column version is needed later, the prompt can be reduced to four panels:
  input series -> projected latent tensor -> FractionalDMD core -> decoded forecast.
